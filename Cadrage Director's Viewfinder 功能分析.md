# Cadrage Director's Viewfinder 功能分析

## 文档目的

本项目参考 Cadrage Director's Viewfinder，在 HarmonyOS 手机上实现面向哈苏相机和指定镜头的数字导演取景器。

项目不追求复制 Cadrage 的全部功能，而是聚焦于哈苏用户的现场构图、镜头比较和拍摄预演。

## 参考资料

- 视频：[Cadrage Director's Viewfinder - App Review 2020](https://www.youtube.com/watch?v=ev0EBiyaoo8)
- 作者评测：[Cadrage Director's Viewfinder - App Review 2020](https://www.danielgrindrod.com/blog/cadrage)
- 官方功能说明：[Cadrage Tech Specs](https://www.cadrage.app/tech-specs/)

说明：YouTube 页面可以确认视频标题和主题，但当前无法获取完整字幕文字稿。本文根据视频对应的作者评测文章和 Cadrage 官方功能说明整理，功能归纳不是视频逐字字幕。

## Cadrage 的产品定位

Cadrage 不是普通相机应用，而是服务于导演、摄影指导和剧组前期准备的数字取景器。它使用手机摄像头模拟专业相机与镜头组合的视野，让用户在不携带实际摄影机和镜头的情况下完成构图、勘景和拍摄预演。

核心价值是：

> 用手机快速确认指定相机和镜头能够看到什么画面。

## 主要功能

### 1. 相机与镜头组合

- 选择专业相机、传感器格式和镜头。
- 根据相机格式和焦距计算视场范围。
- 支持一次选择多个镜头，快速比较不同焦段。
- 支持保存常用的相机与镜头组合。
- 支持镜头转接环、Speed Booster 和变形宽银幕镜头等配置。

### 2. 取景和构图

- 使用手机后置摄像头实时预览。
- 添加任意画幅比例的 Frameline。
- 支持三分法、黄金比例、中心十字、Action Safe、Title Safe 和网格。
- 可以调整辅助线的颜色、粗细和画面外区域的遮罩颜色。
- 支持手机镜头校准，提高模拟精度。

### 3. 记录和现场协作

- 保存照片和视频片段。
- 内置图库、项目和场景管理。
- 给镜头添加标题和备注。
- 创建并导出 PDF Shot List。
- 记录相机格式、焦距、视场、时间、GPS 和罗盘方向等元数据。

### 4. 画面预览和辅助工具

- 手动控制曝光、对焦和白平衡。
- 使用预设或 LUT 预览最终画面风格。
- 进行三路色彩校正。
- 显示太阳路径，辅助判断不同时间的光线方向。
- 支持双摄和三摄手机。

## 本项目的产品边界

本项目定位为：

> 面向哈苏用户的 HarmonyOS 原生 Director's Viewfinder，使用华为手机后置摄像头，模拟指定哈苏相机与 XCD 镜头的视场范围，重点服务于拍摄前构图、镜头比较和现场预演。

明确限制：

- 只支持哈苏相机。
- 只支持项目镜头数据库中列出的哈苏镜头。
- 只使用手机后置摄像头。
- 通过手机不同物理摄像头和变焦倍率模拟视场，不代表真正安装了哈苏镜头。
- 视场模拟依赖手机相机校准，未校准时只能视为估算结果。

## Cadrage 与本项目能力映射

| Cadrage 能力 | 本项目状态 |
| --- | --- |
| 多品牌相机数据库 | 不实现，只支持哈苏 |
| 大型镜头数据库 | 不实现，只支持指定哈苏 XCD 镜头 |
| 手机摄像头取景 | 已实现后置相机预览 |
| 手机变焦控制 | 已实现 `0.5x`、`1x`、`4x`、`6.2x` |
| 变焦后的 FOV 计算 | 已实现基于等效焦距的估算 |
| 多镜头比较 | 已实现镜头多选和多颜色视场框 |
| 画幅选择 | 已实现 `3:3`、`4:3`、`16:9` |
| 构图网格 | 已实现三分网格 |
| 自定义 Frameline | 暂未实现 |
| 手机镜头校准 | 暂未实现 |
| 拍照和录像 | 暂未实现 |
| 图库、项目和场景 | 暂未实现 |
| Shot List 和备注 | 暂未实现 |
| LUT 和色彩预览 | 暂未实现 |
| 太阳路径 | 暂未实现 |

## 当前已实现的核心逻辑

### 手机变焦

当前手机变焦档位：

| 手机倍率 | 模拟等效焦距 |
| --- | --- |
| `0.5x` | `13mm` |
| `1x` | `24mm` |
| `4x` | `90.5mm` |
| `6.2x` | `140mm` |

计算思路是以手机 `1x / 24mm` 作为基准，根据等效焦距通过针孔模型换算当前水平和垂直 FOV，再与哈苏镜头 FOV 对比，绘制目标视场框。

这是一种可用的初始估算模型。要达到现场可靠使用的精度，需要后续加入手机相机校准和实际裁切系数修正。

### 多镜头视场框

- 用户可以展开镜头列表。
- 用户可以同时选择多个哈苏镜头。
- 每个镜头使用独立颜色绘制视场框。
- 底部颜色图例显示焦距，颜色与画面框线保持一致。
- 至少保留一个选中镜头，避免画面没有目标框线。

### 画幅

- 默认画幅为 `16:9`。
- 点击画幅入口后选择 `3:3`、`4:3` 或 `16:9`。
- `3:3` 按正方形 `1:1` 处理。

## 推荐开发路线

### 第一阶段：可靠取景

- 完成华为手机各物理摄像头的 FOV 校准。
- 验证 `0.5x`、`1x`、`4x`、`6.2x` 对应的真实画面变化。
- 修正预览 Surface、屏幕方向和取景框的对齐关系。
- 验证不同画幅下的裁切和黑边策略。
- 完成所有目标哈苏镜头的 FOV 数据复核。

### 第二阶段：记录现场方案

- 增加拍照功能。
- 保存当前镜头、倍率、画幅和时间信息。
- 增加项目、场景和镜头备注。
- 在图库中查看历史取景结果。

### 第三阶段：提高专业使用价值

- 增加手机相机校准页面。
- 支持自定义 Frameline。
- 增加曝光、对焦和白平衡控制。
- 增加 LUT 或简单的色彩预览。
- 生成简单 Shot List 或 PDF。

## 设计原则

- 先保证视场模拟可信，再扩展记录和协作能力。
- 所有镜头、相机和手机参数都应集中在数据模型中，避免散落在页面代码里。
- FOV 计算保持为独立工具模块，便于校准和单元测试。
- 页面组件按职责拆分并优先复用。
- 每个代码文件总行数控制在 500 至 800 行以内，接近上限时按职责拆分。
- 任何“准确”或“等效”的显示都应标注其校准状态，避免把估算结果误认为实拍结果。
