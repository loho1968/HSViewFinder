# HS ViewFinder 改进计划

## 1. 项目定位

HS ViewFinder 是一个运行在 HarmonyOS 手机上的个人数字导演取景器，用手机后置摄像头模拟哈苏相机与指定镜头的视场范围，帮助拍摄前判断构图和镜头选择。

当前产品不是通用相机 App，也不追求复制 Cadrage 的完整能力。第一阶段优先服务于自己的实际拍摄：横屏使用手机，快速比较哈苏 X2D2 与指定镜头的取景范围。

## 2. 需求范围

### 2.1 当前目标

- 只支持哈苏 X2D2。
- 镜头范围暂定为：25mm 定焦、20-35mm 变焦、30-100mm 变焦。
- 变焦镜头不模拟连续变焦，只显示和模拟业务需要的焦段：20、25、30、35、90mm。
- 以横屏拍摄和横屏视场标示为主。
- 使用手机后置摄像头。
- 通过手机摄像头变焦模拟不同焦段，并绘制哈苏镜头视场框。
- 支持多个焦段同时显示，使用不同颜色框线和底部颜色图例区分。
- 跟随系统深色/亮色模式，不固定为深色主题。

### 2.2 后续目标

- 支持竖屏拍摄和竖拍视场标示。
- 根据拍摄场景估算白平衡/色温。
- 拍照后一次性根据时间、地点和手机拍摄方向计算太阳、月球和银河中心轨迹，并叠加到照片。
- 太阳轨迹区分白天、日出和日落；日出日落使用适合现场拍摄的时间窗口，不使用过长的天文暮光作为首版边界。
- 太阳、月球和银河中心支持独立选择，可选择显示其中 1 个、2 个或 3 个。
- 增加“标记”功能，保存拍摄照片、时间、位置和镜头信息。
- 照片本机保存，文件名包含国家、城市、日期和分钟；同一分钟重复拍摄时追加序号。
- 在照片 EXIF 中写入 GPS 经纬度等位置信息；没有权限或定位失败时仍允许保存，但不写入 GPS。
- 为增加其他相机和镜头预留数据模型与选择框架。

## 3. 截图参考结论

参考截图展示了 Cadrage 的主要交互和信息层级：

- 主画面以实时预览为中心，黑色区域用于承载横屏取景界面。
- 画面内叠加多个不同焦段的矩形框，并在左上角标识焦段。
- 底部工具栏常驻，包含菜单、图库/记录、相机格式、焦段切换和拍摄按钮。
- 点击菜单后，从左侧弹出设置面板，不离开当前取景画面。
- 镜头设置区显示镜头类型、镜头范围和适配器信息。
- 定焦镜头和变焦镜头分开选择。
- 变焦镜头列表显示可选的焦段范围。
- 拍摄结果图的底部可以包含应用名、时间、格式、FOV、焦段和方向信息。

本项目应借鉴其“取景画面优先、工具栏辅助、设置面板覆盖”的信息架构，但保持更小的功能范围和更直接的哈苏专用体验。

## 4. 第一阶段：横屏可靠取景

目标是先让应用在 X2D2 用户的横屏拍摄场景中稳定、清晰、可信。

### 4.1 页面结构

建议拆分为以下可复用组件：

- `ViewfinderPage`：页面生命周期、状态协调和组件组合。
- `CameraPreview`：后置相机预览、Surface 生命周期和相机资源释放。
- `ViewfinderOverlay`：画幅框、网格、镜头视场框和焦段标签。
- `LensSelector`：定焦/变焦镜头分类、焦段选择和多选状态。
- `ZoomSelector`：手机物理摄像头/变焦倍率选择。
- `AspectRatioSelector`：`16:9`、`4:3`、`3:3` 选择。
- `ViewfinderToolbar`：横屏底部工具栏。
- `SettingsPanel`：菜单面板，后续承载相机、镜头、校准和显示设置。
- `LensLegend`：镜头颜色、焦段和镜头名称图例。

组件应尽量通过数据和回调复用，不在页面中重复绘制选择项和状态逻辑。

### 4.2 默认交互

- 启动后直接进入横屏取景页面。
- 默认相机为后置相机。
- 默认手机倍率为 `1x (24mm)`。
- 默认输出画幅为 `16:9`。
- 默认显示 `25mm` 镜头框。
- 点击手机倍率摘要后展开 `0.5x (13mm)`、`1x (24mm)`、`4x (90.5mm)`、`6.2x (140mm)`。
- 点击画幅摘要后展开 `3:3`、`4:3`、`16:9`。
- 点击镜头摘要后展开镜头选择列表。
- 镜头支持多选，至少保留一个镜头。
- 选中的每个镜头使用固定颜色，颜色同时用于框线、焦段标签和底部图例。
- 设置面板和选择列表不应永久遮挡主要取景区域。

### 4.3 镜头数据

相机和镜头信息必须使用独立数据模型，页面不得硬编码镜头计算参数。

```text
CameraSpec
  id
  name
  sensorWidth
  sensorHeight
  supportedLensIds

LensSpec
  id
  name
  type: prime | zoom
  focalLengthMin
  focalLengthMax
  displayFocalLengths
  horizontalFovByFocalLength
  verticalFovByFocalLength
```

当前镜头配置：

| 镜头 | 类型 | 第一阶段显示焦段 |
| --- | --- | --- |
| XCD 2.5/25V | 定焦 | 25mm |
| XCD 20-35 | 变焦 | 20mm、35mm |
| XCD 30-100 | 变焦 | 30mm、90mm |

数据模型要支持后续将 `90mm` 改为 `100mm`，而不需要修改 UI 和 FOV 计算代码。这里的 `90mm` 按当前产品需求作为显示焦段，后续需确认是否应使用变焦镜头的真实最长端 `100mm`。

### 4.4 FOV 计算与校准

计算链路统一为：

1. 根据手机当前倍率取得实际或校准后的手机水平/垂直 FOV。
2. 根据哈苏 X2D2 传感器尺寸和镜头焦距计算目标 FOV。
3. 根据手机当前预览区域的方向和画幅比例计算目标框。
4. 在 Canvas 中绘制框线、焦段标签和图例。

手机倍率的初始映射：

| 手机倍率 | 当前估算等效焦距 |
| --- | --- |
| `0.5x` | `13mm` |
| `1x` | `24mm` |
| `4x` | `90.5mm` |
| `6.2x` | `140mm` |

第一阶段必须增加校准数据入口，至少保存：

- 手机型号。
- 物理摄像头标识。
- 变焦倍率。
- 水平 FOV 和垂直 FOV。
- 预览输出尺寸和旋转方向。
- 校准日期和校准说明。

未校准时，界面应明确显示“估算”状态；校准完成后才显示“已校准”。

### 4.5 横屏适配

- 第一阶段进入页面时优先锁定横屏，退出页面时恢复系统方向。
- 预览 Surface、Canvas 和底部工具栏使用同一横屏坐标系。
- 旋转期间避免旧 Surface 和旧 Canvas 同时绘制。
- 预览 Profile 按 Surface 宽高比选择，避免拉伸。
- 对安全区、状态栏和底部导航区域进行统一处理。
- 横屏分辨率变化时重新计算取景框，不使用固定像素坐标。

## 5. 第二阶段：拍摄与标记

当第一阶段的 FOV 可信后，增加现场记录能力。

### 5.1 拍摄

- 增加拍照按钮，保存手机后置摄像头拍摄的原始照片。
- 点击快门时一次性记录拍摄时间、GPS、海拔、手机方向、横竖屏状态、手机镜头、手机变焦、相机焦段和画幅比例。
- 建议同时保存原图和带框线/分析结果的标注图，原图不得被叠加绘制覆盖。
- 元数据包含相机、镜头、焦段、手机倍率、画幅、时间、时区、方向、东西南北参考、定位精度和校准状态。
- 色温与天文计算统一放入拍照后的分析任务，不参与实时预览绘制。

### 5.2 标记

标记记录建议包含：

- 拍摄时间和时区。
- 国家、城市、经纬度和定位精度。
- 当前场景名称或用户备注。
- 哈苏相机型号。
- 镜头和焦段。
- 手机倍率和画幅。
- 罗盘方向。

文件名使用可读且稳定的格式，例如：

```text
中国-上海-20260904-1530.jpg
中国-上海-20260904-1530-01.jpg
```

文件名中的国家和城市需要处理空格、路径分隔符、特殊字符和定位失败场景；GPS 不可用时使用 `Unknown-Unknown`，不能阻止照片保存。文件名精确到分钟，同一分钟重复拍摄时追加 `-01`、`-02` 等序号。

### 5.3 GPS 与照片元数据

- GPS 不因获得位置权限而自动保证写入照片，保存流程必须显式写入并验证 JPEG EXIF。
- 写入纬度、经度、方向、海拔和 GPS 时间；拍摄方向可作为可选 EXIF 或结构化元数据保存。
- 用户拒绝定位权限、关闭位置记录或定位失败时，照片仍正常保存，但不伪造 GPS 数据。
- 应用专有信息（所选焦段、FOV 校准版本、天文计算结果和色温置信度）同时保存为结构化元数据，不能全部依赖标准 EXIF。

## 6. 第三阶段：天文和环境辅助

### 6.1 拍照后分析总流程

拍照后创建一次分析任务，统一处理色温和天文信息：

```text
拍摄原图
  ↓
记录 CaptureContext
  ↓
并行计算色温、天体位置和轨迹
  ↓
将结果投影到照片坐标
  ↓
生成标注图并写入照片元数据
  ↓
保存原图、标注图和结构化分析结果
```

实时取景阶段不显示太阳、月球或银河中心轨迹，只显示已有的镜头视场框和辅助线。

### 6.2 白平衡/色温估算

- 先显示估算色温，不直接改变相机白平衡。
- 明确区分“环境光色温估算”和“照片白平衡设置”。
- 记录估算值、估算来源和置信度。
- 在室内混合光、夜景和极端光线下允许显示“无法可靠估算”。

### 6.3 太阳、月球和银河

输入数据：

- GPS 经纬度。
- 日期和时间及时区。
- GPS 海拔和定位精度。
- 拍摄瞬间手机方向传感器和罗盘方向。
- 手机横屏/竖屏状态、后置摄像头安装方向和镜头变焦。
- 当前预览 FOV、裁切方式和哈苏 X2D2 画幅/镜头参数。

方向计算必须使用拍摄时的手机朝向，而不是仅计算天体在天空中的位置：

```text
天体位置
  ↓ 时间 + GPS
东南西北方位和高度角
  ↓ 真北方位 + 手机方位/俯仰/横滚
相机坐标
  ↓ 手机镜头 FOV + 画面旋转/裁切
照片像素坐标
```

方向数据至少包含：

- 方位角：北 `0°`、东 `90°`、南 `180°`、西 `270°`。
- 俯仰角和横滚角。
- 磁北到真北的修正值。
- 方向传感器精度和校准状态。

轨迹选择和可见性规则：

- 太阳、月球、银河中心分别提供独立开关，可选择其中 1 个、2 个或 3 个。
- 根据拍摄时间、地点和天体高度判断当前是否可见。
- 白天默认主要显示太阳；月球在满足可见条件时显示；银河中心在夜间满足可见条件时显示。
- 当前不可见的天体不绘制轨迹，并在分析结果中记录“当前不可见”及原因。
- 每条轨迹显示当前时刻位置点；太阳额外标记日出或日落状态。

太阳分支和摄影窗口：

- `白天`：太阳在地平线以上的普通时段。
- `日出`：日出前约 60 分钟至日出后约 90 分钟。
- `日落`：日落前约 90 分钟至日落后约 60 分钟。
- 首版不以过长的天文暮光作为拍摄窗口边界，后续可增加摄影窗口配置。

显示内容：

- 太阳、月球和银河中心的方位、高度角及当前可见状态。
- 太阳的日出、日落摄影窗口。
- 月球的月相、月出月落信息。
- 银河中心的方位和可见时间窗口。
- 照片中的方向参考线或标记：`N`、`E`、`S`、`W`。

天文计算和投影应拆分为独立的 `AstronomyService`、`SkyProjection` 和 `AstronomyOverlayRenderer`，不能直接写入取景页面。

## 7. 第四阶段：扩展相机与镜头框架

虽然当前只支持 X2D2，但数据结构从第一天就按可扩展方式设计：

- 相机、传感器格式和镜头分离建模。
- 相机通过 `supportedLensIds` 关联镜头。
- FOV 计算不依赖具体品牌字符串。
- UI 允许未来增加相机选择入口，但第一阶段可以隐藏。
- 变焦镜头的连续范围和业务显示焦段分开存储。
- 兼容新镜头时只增加数据，不修改通用绘制组件。

## 8. 深色/亮色模式

- 颜色使用系统主题资源或统一 Design Token，不在组件中散落硬编码主题颜色。
- 相机画面上的框线颜色需要在深色和亮色背景上都保持可辨识。
- 镜头颜色应固定，不因主题切换而改变语义。
- 面板、工具栏、文字和按钮跟随系统主题。
- 叠加层的遮罩透明度应根据主题调整，避免亮色模式下过暗或深色模式下刺眼。
- 截图参考中的深色工具栏只作为一种视觉参考，不能成为产品的固定主题。

## 9. 代码组织要求

- 每个代码文件总行数控制在 500 至 800 行以内。
- 页面只负责状态协调和布局组合，不承载完整的相机、FOV、定位或天文计算。
- 可复用组件使用独立文件，选择器、图例、工具栏和面板避免重复实现。
- 相机生命周期集中在相机模块中，页面退出时统一释放资源。
- FOV 计算、倍率换算和坐标转换保持为纯工具模块，便于单元测试。
- 数据模型与显示文案分离，避免用 UI 文本作为业务判断条件。
- 新增功能先判断是否能复用现有组件和数据结构，再决定是否新增文件。

## 10. 阶段验收标准

### M1：横屏取景 MVP

- 真机启动后自动进入横屏后置预览。
- `16:9`、`4:3`、`3:3` 画幅切换正常。
- `0.5x`、`1x`、`4x`、`6.2x` 变焦切换正常。
- 25mm、20mm、30mm、35mm、90mm 可选择和多选。
- 多个视场框颜色稳定，图例与框线对应。
- 预览无明显拉伸、黑屏、旋转错位和 Surface 残留。
- 深色和亮色模式均可正常阅读和操作。
- 关键代码文件符合 500 至 800 行规范。

### M2：现场标记

- 可拍照并保存原图和标注图。
- 色温与天文分析在拍照后执行，不阻塞实时预览。
- 可保存结构化拍摄信息，包括时间、GPS、方向、东西南北参考、焦段和 FOV。
- GPS 可用和不可用时都能完成保存。
- GPS 可用时可在照片 EXIF 中读取经纬度；关闭或拒绝权限时不写入 GPS。
- 文件名按“国家-城市-日期-时间到分钟”生成，同一分钟不会覆盖已有照片。
- 图库可以查看并区分不同场景。

### M3：环境与天文

- 色温估算结果标注估算状态和置信度。
- 太阳、月球和银河中心支持 1/2/3 条轨迹选择。
- 太阳正确区分白天、日出和日落摄影窗口。
- 太阳、月球和银河中心轨迹根据拍摄瞬间的时间、地点和手机方向投影到照片。
- 东西南北方向和磁北到真北修正正确参与投影。
- 无定位、无罗盘或权限不足时有明确降级提示，不阻止原图保存。

## 11. 当前优先级

1. 把页面重构为横屏取景器布局：预览区、底部工具栏、可复用选择面板。
2. 将镜头数据收敛为 X2D2 的 25、20-35、30-100 三组配置。
3. 完成手机各物理摄像头和倍率的 FOV 校准。
4. 增加拍照、原图/标注图保存和 EXIF GPS 写入。
5. 增加系统深色/亮色主题适配。
6. 增加拍照后色温分析。
7. 增加拍照后太阳、月球、银河中心轨迹计算和方向投影。
8. 再开始扩展相机框架和其他后续能力。

## 12. 待办清单

以下项目尚未完成，后续开发按顺序继续：

- [x] 真机后置相机预览和横竖屏恢复。
- [x] 哈苏 X2D2 镜头视场框、多选和手机变焦选择。
- [x] 独立“色温和日月”页面及太阳、月亮、银河中心选择菜单。
- [x] 拍照并保存到系统图库。
- [x] 建立拍摄上下文，记录拍摄时间、手机画面尺寸、手机变焦和选择的镜头。
- [x] 拍照时尝试获取 GPS，并将位置传入相机拍摄设置。
- [ ] 验证照片 GPS 是否实际写入 JPEG EXIF，并补充 EXIF 读回校验。
- [ ] 获取拍摄瞬间手机方位角、俯仰角和横滚角。
- [ ] 增加东西南北方向显示，并完成磁北到真北的修正。
- [ ] 完成拍照后白平衡/色温估算及置信度显示。
- [ ] 完成太阳位置、日出和日落摄影窗口计算。
- [ ] 完成月亮位置、月相和月出月落计算。
- [ ] 完成银河中心位置和可见时间窗口计算。
- [ ] 将太阳、月亮和银河中心轨迹投影到拍摄照片坐标。
- [ ] 生成原图与叠加标注图，并保存分析结果。
- [ ] 实现“国家-城市-日期-时间到分钟”的自定义文件名及重复拍摄序号。
- [ ] 将分析任务放入后台执行，避免阻塞拍照和页面交互。
- [ ] 用真机完成色温、GPS、方向和三类天文轨迹回归测试。
