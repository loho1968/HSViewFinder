# 项目记忆

## HarmonyOS 工具链

- 主力 DevEco Studio：26.0.0 Beta1 / .461
- 主力内置 SDK：HarmonyOS 26.0.0.23，apiVersion 26
- 独立 Command Line Tools：6.0.2.660，内置 HarmonyOS 6.0.2.130，apiVersion 22
- 工程目标 SDK：HarmonyOS 26.0.0，apiVersion 26（鸿蒙 7.0）
- 设备：HUAWEI Mate 80 RS | ULTIMATE DESIGN，Serial `5YZ0225C02003196`

## 强制约定

- 不使用 PATH 中的 `hvigorw`、`ohpm` 或其他可能指向旧 Command Line Tools 的工具进行项目构建。
- 项目构建必须使用 DevEco Studio 26 内置 Node 与 hvigor 的绝对路径：
  `/Applications/DevEco-Studio.app/Contents/tools/node/bin/node`
  `/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw.js`
- CLI 构建时须显式设置 `DEVECO_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk`
  （指向 `default` 的父目录，不是 `Contents/sdk/default`），否则报错 00303217 / 00303312。
- DevEco Studio、命令行工具和工程 SDK 必须保持同一主版本，避免编译产物与 IDE 运行环境不一致。

## 代码组织规范

- 每个代码文件总行数控制在 500 至 800 行以内；接近上限时按职责拆分，低于 500 行时也不为了凑行数而堆叠代码。
- 项目代码按职责合理组织，页面、业务逻辑、数据模型和工具能力保持清晰边界。
- 新增能力前先检查现有组件、Builder、工具类、数据模型和状态逻辑；能复用就复用，避免重复实现同类能力。
- 可复用的 UI、业务、几何和状态能力应抽取到通用组件或独立模块，页面只负责组合与编排。
- 修改或新增源码前优先通过 Codegraph-arkts 查询现有符号、调用关系和影响范围，避免重复创建已有能力。
- 读取/修改代码前优先用 Codegraph-arkts 索引（`node /Users/lh/Developer/Demo/codegraph-arkts/dist/bin/codegraph.js`），
  常用命令 `status / query / explore / node / files`；改动源码后运行 `sync`（或 `index`）更新索引。
