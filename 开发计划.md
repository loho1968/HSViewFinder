# HSViewFinder 开发计划

## 1. 项目目标

构建一个 HarmonyOS 原生 ArkTS 取景器应用，第一阶段只围绕哈苏 X2D2 和哈苏 XCD 镜头，提供：

1. 实时预览。
2. 相机设备切换。
3. 在预览流上叠加 Canvas 自绘取景框。
4. 按镜头焦段显示不同视场框。
5. 显示 3:2、4:3、16:9 等比例辅助线。
6. 在屏幕旋转、应用前后台切换和预览尺寸变化时保持叠加层与画面对齐。

第一阶段定位为 ViewFinder/Kader 类的取景辅助工具，不包含照片保存、RAW 处理、完整相机控制台或云端账号体系。

## 2. 当前工程基线

当前工程是一个可构建的 HarmonyOS Stage 工程，代码仍是默认模板，适合从相机能力验证开始演进。

- 主模块：`entry`
- UIAbility：`EntryAbility`
- 页面：`entry/src/main/ets/pages/Index.ets`
- 应用包名：`com.loho.myapplication`
- 目标 SDK / 兼容 SDK：`26.0.0`
- 本地构建工具链：Hvigor `6.26.1`，Toolchains `26.0.0.23`
- 当前构建状态：调试构建已通过，出现 `BUILD SUCCESSFUL`
- 当前设备状态：没有连接设备；本机有 5 个 HarmonyOS 6.1.0(23) 模拟器
- 当前限制：工程没有签名配置，当前产物为未签名 HAP
- 镜头数据状态：`哈苏镜头.md` 已包含 18 条镜头记录及 HFOV/VFOV
- X2D2 几何参数状态：有效传感器尺寸已提供为 `43.8 × 32.9 mm`

相关配置：

- [build-profile.json5](/Users/lh/Developer/HSViewFinder/build-profile.json5)
- [entry module.json5](/Users/lh/Developer/HSViewFinder/entry/src/main/module.json5)
- [哈苏镜头.md](/Users/lh/Developer/HSViewFinder/哈苏镜头.md)

## 3. 先决条件与可行性闸门

### 3.1 明确预览输入和“多摄切换”的含义

本项目的 MVP 预览输入固定为手机或平板的 CameraKit 摄像头。手机镜头负责拍摄当前场景，哈苏镜头数据只负责生成目标视场框和镜头选择建议。

因此，产品中的两个切换概念需要分开：

- **手机相机切换**：切换 CameraKit 返回的后置超广角、广角、长焦或其他可用摄像头，用来获得更适合的参考视场。
- **哈苏镜头选择**：切换镜头目录中的目标 HFOV/VFOV，Canvas 在手机预览画面上绘制相应视场框，不会切换真实硬件镜头。

X2D2 不作为第一版的实时输入源，也不要求 CameraKit 直接控制 X2D2。将来如果需要把 X2D2 的实时画面接入应用，再单独增加外部取流适配层，不影响本机预览和几何模块。

**闸门 G0 的通过条件**：在至少一台目标 HarmonyOS 设备上确认 CameraKit 能枚举并启动手机摄像头，取得每个 Preview Profile/变焦状态下的实际预览 FOV、宽高比、裁切和旋转信息。外部 X2D2 协议不再是 MVP 的阻塞条件。

### 3.2 X2D2 几何参数基线

当前已具备用于第一版物理视场角计算的核心数据：

| 参数 | 值 | 用途 |
|------|----|------|
| 有效传感器宽度 | 43.8 mm | 水平 FOV 计算 |
| 有效传感器高度 | 32.9 mm | 垂直 FOV 计算 |
| 有效像素 | 11656 × 8742 | 输出比例和像素映射参考 |
| 像素边长 | 3.76 μm | 后续像素级标定参考，不参与基础 FOV 计算 |
| 裁切系数 | 约 0.79 - 0.8 | 仅作为 35mm 等效显示参考，不替代实际传感器尺寸 |
| 传感器类型 | BSI CMOS | 记录为设备规格，不参与基础 FOV 计算 |
| 动态范围 | 15 档 | 后续曝光/画质信息展示，不参与基础 FOV 计算 |
| 色深 | 16 bit | 后续成像/输出能力评估，不参与基础 FOV 计算 |

`哈苏镜头.md` 现在还提供了每支镜头的 HFOV/VFOV。计划采用“双重校验”策略：

- 运行时的基础计算使用实际焦距和 `43.8 × 32.9 mm` 有效传感器尺寸。
- Markdown 中的 HFOV/VFOV 作为已知参考值，用于显示、回归测试和数据一致性检查。
- 对变焦镜头保存广角端和长焦端 FOV；连续焦段按实际焦距重新计算，不对角度做线性插值。
- 等效焦距仅用于界面展示和与 35mm 规格对照，不能作为 X2D2 FOV 计算输入。

X2D2 参数现在作为目标镜头的几何基线；Phase 0/Phase 1 还需要建立手机预览基线，包括手机相机的水平/垂直 FOV、变焦状态、输出比例、预览裁切、电子防抖裁切、畸变校正和旋转方向。

**闸门 G1 的通过条件**：X2D2 传感器和镜头表几何校验通过，并完成至少一个手机摄像头 Profile 的 FOV 标定；用广角、标准和中长焦目标镜头确认手机预览能否完整覆盖其视场。

当前数据预检结果：18 条镜头记录均可解析，按 `43.8 × 32.9 mm` 和实际焦距复算 HFOV/VFOV，最大偏差约 `0.53°`，在表格保留一位小数的合理舍入范围内。该结果足以支撑第一版 FOV 计算；后续仍需以实际预览流确认裁切和校准偏差。

## 4. 技术架构

### 4.1 分层

```text
Index / ViewFinderPage
  ├─ PreviewSurface                 手机 CameraKit XComponent Surface
  ├─ OverlayCanvas                  透明 Canvas，绘制视场框与辅助线
  ├─ CameraControls                 镜头、比例、相机切换和状态提示
  └─ ViewFinderViewModel             页面状态与生命周期协调

CameraSource
  └─ CameraKitPreviewSource          HarmonyOS 手机/平板 CameraKit

CameraSessionController
  ├─ CameraManager / CameraInput
  ├─ PreviewOutput / Session
  ├─ 手机摄像头枚举、切换、重启和释放
  └─ 权限、旋转、前后台和错误处理

Geometry
  ├─ LensCatalog                      镜头数据
  ├─ PhoneCameraProfile               手机预览 FOV/裁切/变焦基线
  ├─ FovCalculator                    视场角计算
  ├─ PreviewTransform                  Surface 到屏幕的映射
  └─ OverlayLayoutCalculator           视场框、比例框和标签布局
```

### 4.2 CameraKit 预览链路

按官方 CameraKit 预览流程实现，不直接从底层 `hdc` 或 `hilog` 开始：

1. 在 `entry/src/main/module.json5` 声明 `ohos.permission.CAMERA`。
2. 运行时通过 `AtManager.requestPermissionsFromUser()` 请求权限，并处理拒绝和设置页回退。
3. 通过 `camera.getCameraManager(context)` 获取相机管理器。
4. 通过 `getSupportedCameras()` 枚举手机摄像头，按设备 ID、位置和能力筛选目标相机。
5. 使用 `XComponent` 的 `SURFACE` 类型创建预览 Surface，获取 `surfaceId`。
6. 根据屏幕宽高比、相机安装方向和支持能力选择 `previewProfile`。
7. 创建 `CameraInput`、`PreviewOutput` 和相机会话 `Session`。
8. 调用 `beginConfig()`、`addInput()`、`addOutput()`、`commitConfig()`、`start()` 启动预览。
9. 监听 `frameStart`、`frameEnd`、设备状态和错误事件。
10. 切换相机或页面离开时按 `stop -> output.release -> input.close -> session.release` 释放资源。

预览 Surface 和输出 Profile 必须保持宽高比一致。对于 `cover` 裁切，几何层必须知道实际可见内容矩形，不能直接把 Canvas 的整个组件矩形当作相机成像矩形。

### 4.3 Canvas 覆盖层

预览层和 Canvas 层使用相同的父容器、相同的布局尺寸和相同的旋转策略：

- 底层：手机 CameraKit 的 XComponent Surface。
- 顶层：透明 Canvas，使用 `CanvasRenderingContext2D` 绘制。
- UI 控件：只覆盖在需要交互的位置，避免改变预览 Surface 的尺寸。
- 每次镜头、比例、屏幕尺寸、旋转或裁切参数变化时，重新计算并重绘。
- 绘制前使用 `clearRect`，用 `save/restore` 隔离不同线型、透明度和颜色。
- Canvas 只负责绘制；焦段、比例和投影计算放在纯数据模块，便于单元测试。

## 5. 镜头数据模型

`哈苏镜头.md` 当前包含 18 条 XCD 镜头记录，既有定焦，也有变焦。建议保留 Markdown 作为人工可读来源，同时在工程内维护可被程序加载的规范化数据文件。

建议模型：

```ts
interface LensSpec {
  id: string;
  series: string;
  name: string;
  focalLengthMm: number | { min: number; max: number };
  equivalentFocalLengthMm?: number | { min: number; max: number };
  horizontalFovDeg?: number | { wide: number; tele: number };
  verticalFovDeg?: number | { wide: number; tele: number };
  maxAperture?: number | { wide: number; tele: number };
  minFocusDistanceM?: number;
  sourceNote?: string;
}

interface SensorSpec {
  id: string;
  widthMm: number;
  heightMm: number;
  pixelWidth: number;
  pixelHeight: number;
  pixelSizeUm?: number;
  equivalentCropFactor?: number;
  outputAspectRatios: number[];
  cropMode: 'center' | 'device-defined';
}
```

数据清洗任务：

- 将单焦段和变焦焦段统一为可计算的范围结构。
- 对 `XCD 4/45P` 的 `46.2mm`、`XCD 4/21` 的直径字段等疑似录入异常保留原值并增加 `sourceNote`，不要静默修正。
- 将表头中的“最小光圈”核对为“最大光圈/全开光圈”或其他准确含义。
- 对重复或相近焦段使用稳定的 `id`，不能用显示名称作为唯一键。
- 录入 X2D2 传感器规格、HFOV/VFOV 校验基线、输出比例、裁切方式和数据来源版本。
- 变焦镜头在 MVP 中先提供端点焦段；连续焦段和焦环联动作为后续能力。

## 6. 视场角与屏幕投影

### 6.1 基础视场角

对于有效传感器尺寸 `sensorWidth = 43.8 mm`、`sensorHeight = 32.9 mm` 和实际焦距 `focalLength`：

```text
FOVx = 2 * atan(sensorWidth  / (2 * focalLength))
FOVy = 2 * atan(sensorHeight / (2 * focalLength))
```

计算结果统一使用弧度，界面展示时再转换为角度。对于指定预览比例，如果系统对传感器画面进行了居中裁切，先计算裁切后的有效传感器宽高，再计算 FOV。

已提供的 HFOV/VFOV 可用于验证公式。例如 38 mm 镜头按完整有效传感器计算约为水平 59.9°、垂直 46.8°，与镜头表一致。实现时应保留一个可配置误差阈值；如果焦距、传感器尺寸和表中 FOV 不一致，进入数据校验日志，不要静默覆盖原始数据。

### 6.2 手机预览基准

手机预览不是 X2D2 传感器的直接成像，因此几何模块需要为每个可用手机摄像头和关键变焦状态保存独立的预览 Profile：

```ts
interface PhoneCameraProfile {
  cameraId: string;
  label: string;
  horizontalFovDeg: number;
  verticalFovDeg: number;
  previewWidth: number;
  previewHeight: number;
  zoomRatio: number;
  cropMode: 'center' | 'device-defined';
  stabilizationCrop?: number;
  calibrationSource: 'camera-metadata' | 'measured' | 'user-calibrated';
}
```

Profile 的优先级为：官方/系统可提供的能力和元数据、实测参考物、用户校准。CameraKit 的 Zoom Ratio 不能直接当作线性 FOV 倍率；每个设备和摄像头的 FOV 变化需要通过 Profile 或标定记录。

### 6.3 从相机画面到屏幕

投影流程固定为：

1. 根据输出 Profile 和 Surface 比例，计算预览内容矩形。
2. 处理 `contain`、`cover` 或设备定义的裁切，得到内容矩形的 `left/top/width/height`。
3. 将相机坐标归一化到 `[0, 1]`，按屏幕旋转和相机安装角度转换坐标轴。
4. 将归一化坐标映射到内容矩形，而不是直接映射到 Canvas 外框。
5. 对取景框和比例线做边界裁剪，避免刘海、圆角和安全区造成误读。

### 6.4 镜头视场框与选择建议

当前手机预览 Profile 已知时，目标哈苏镜头的框可以按半角正切比缩放：

```text
frameWidthRatio  = tan(hasselbladHFOV / 2) / tan(phonePreviewHFOV / 2)
frameHeightRatio = tan(hasselbladVFOV / 2) / tan(phonePreviewVFOV / 2)
```

然后以手机预览内容矩形中心生成目标框。镜头选择器同时判断：

- 目标 HFOV 不大于手机预览 HFOV，且目标 VFOV 不大于手机预览 VFOV：可以完整显示目标框。
- 目标视场超过手机当前预览视场：目标框会超出屏幕，显示“当前手机镜头无法完整模拟”，并建议切换手机超广角镜头。
- 手机预览明显大于目标镜头视场：可以用内框模拟目标镜头，框外保留手机实际看到的画面作为环境参考。

该框只能称为“取景模拟”，不能称为 X2D2 的实时成像。只有在以下条件成立时，取景框才具有可验证的物理准确度：

- 手机预览输入的真实水平/垂直 FOV 已知或已标定。
- 预览的裁切、电子防抖、畸变校正和缩放状态已知。
- 哈苏镜头的 HFOV/VFOV 与目标传感器基线一致，或已通过标定修正。

默认界面应标识“模拟视场”；完成手机 Profile 标定后可标识“已标定模拟视场”。

### 6.5 比例辅助线

比例线不是镜头 FOV 框。建议独立提供：

- 3:2：与 X2D2 原生画幅对应的基准框。
- 4:3：上下或左右按内容矩形居中裁切。
- 16:9：上下或左右按内容矩形居中裁切。
- 自定义比例：输入正数宽高，统一走同一套裁切和投影算法。

第一版辅助线包括外框、三等分网格和中心十字；黄金分割、对角线、水平仪和安全框放到后续迭代。

### 6.6 屏幕方向与取景区策略

#### 结论

X2D2 的原生画幅为 3:2，摄影取景通常以横向构图为主；但本项目的实际输入是手机摄像头，手机在日常使用中更常保持竖屏，镜头选择、比例切换和状态提示也需要持续可操作。因此第一版不强制整个应用横屏，也不把手机屏幕简单旋转 90° 当作相机屏幕。

采用“两种显示模式、一个几何内核”的方案：

- **竖屏取景模式（MVP 默认）**：应用保持竖屏，预览区位于页面主体并尽量占据可用高度；底部保留镜头和画幅控制。3:2、4:3、16:9 框均在预览内容矩形内按真实比例绘制，不能拉伸画面填满外框。
- **横屏沉浸模式（第二阶段）**：用户主动进入横屏取景，应用通过窗口方向 API 切换为横屏，隐藏或收纳大部分控制项，把屏幕优先留给 3:2 取景。退出时恢复进入前的方向，不影响系统自动旋转设置。

#### 针对当前屏幕的布局判断

屏幕物理分辨率为 `2848 × 1320`，竖屏逻辑尺寸约为 `1320 × 2848`，屏幕比例约 `2.16:1`。因此：

- 竖屏时，建议预览区使用屏幕宽度作为基准，3:2 主取景框约为 `1320 × 1980`，剩余高度用于顶部状态和底部控制；实际尺寸由安全区和窗口 vp 动态计算。
- 横屏时，3:2 取景框可以接近全屏宽度，高度约为屏幕宽度的 `2/3`，上下只保留少量状态或控制区域，符合相机屏幕的观看习惯。
- 不建议第一版把整个竖屏屏幕旋转成横向预览，也不建议用 16:9 画面强行铺满屏幕后再叠加 3:2 框；前者会损失交互空间，后者会造成画面比例误判。

#### 预览区与控制区边界

预览画面不必永远占满整个屏幕，但必须占满“预览容器”并保持 CameraKit 输出比例。Canvas 只覆盖预览容器，不覆盖底部控制区；取景框、比例辅助线和手机画面使用同一个 `contentRect`，避免控制区变化导致框线漂移。

预览容器采用以下优先级：

1. 先避开状态栏、挖孔、圆角和底部手势安全区。
2. 在剩余空间内按当前预览 Profile 和目标画幅计算最大可用矩形。
3. 对 3:2 主框以居中显示为默认；4:3/16:9 作为独立裁切框，不改变底层预览的真实比例。
4. 控件使用叠加胶囊或底部抽屉形态，展开时减少预览高度，不能覆盖取景框中心区域。

#### 方向状态模型

页面状态需要区分以下三个概念：

- `windowOrientation`：应用窗口当前是竖屏还是横屏。
- `previewOrientation`：CameraKit 输出和 Surface 的旋转方向。
- `compositionAspect`：用户正在模拟的 3:2、4:3 或 16:9 构图比例。

三者不能混用。切换窗口方向时，需要重新获取窗口尺寸、更新 Preview Profile/旋转变换、重新计算 `contentRect` 并重绘 Canvas；窗口方向变化不应改变用户选择的哈苏镜头和构图比例。

#### 当前屏幕参数的使用边界

2848×1320 用于窗口尺寸和布局断点；445 PPI、双层 OLED、2.5D 微曲和超广色域用于后续显示效果与验收记录，不参与 FOV 几何换算。挖孔和圆角只影响安全区与可视布局，不改变传感器视场计算。

## 7. 分阶段实施计划

### Phase 0：手机预览可行性与数据冻结

交付物：技术决策记录、手机预览 Profile 标定结论、冻结版 `SensorSpec`、规范化镜头数据。

- 确认 MVP 使用手机/平板 CameraKit 预览，X2D2 只作为目标镜头与传感器数据源。
- 将已确认的 `43.8 × 32.9 mm`、`11656 × 8742`、3.76 μm 和 HFOV/VFOV 写入版本化配置。
- 采集手机 CameraKit 返回的摄像头 ID、方向、支持 Profile、变焦范围和输出比例。
- 为至少一个手机摄像头建立水平/垂直 FOV、预览裁切、电子防抖裁切和畸变校正基线。
- 清洗 `哈苏镜头.md` 数据，保留疑似异常值和来源备注。
- 对焦距计算值与 HFOV/VFOV 参考值执行自动一致性检查。
- 明确第一版支持的镜头列表、变焦端点和比例列表。
- 确认竖屏取景为 MVP 默认，横屏沉浸取景列入第二阶段；冻结预览容器、控制区和安全区的布局规则。

验收：手机预览和 X2D2 目标镜头的几何基线冻结；可以对每个目标镜头判断“可完整模拟 / 只能部分模拟 / 当前手机镜头不可模拟”。

### Phase 1：CameraKit 最小预览闭环

交付物：可授权、可启动、可停止的本机实时预览页面。

- 增加 Camera 权限声明与运行时权限流程。
- 实现 `CameraKitPreviewSource` 和 `CameraSessionController`。
- 使用 XComponent Surface 承载 `PreviewOutput`。
- 选择与 Surface 比例匹配的 Preview Profile，处理设备旋转。
- 实现启动、停止、错误提示和页面销毁释放。
- 在真机上记录首帧时间、预览分辨率、实际 FOV 和黑屏情况；模拟器只作为流程回归环境。

验收：用户授权后能看到稳定预览；拒绝权限、切后台、旋转屏幕、重复进入页面不崩溃、不泄漏相机资源。

### Phase 2：相机设备枚举与切换

交付物：设备相机列表和切换能力。

- 展示 CameraKit 返回的手机摄像头 ID、位置、方向和支持能力。
- 切换前停止旧 Session，释放旧输入/输出，再创建新 Session。
- 防止并发点击导致重复切换；切换过程中显示忙碌状态。
- 监听手机摄像头被系统占用、设备断开和前后台状态变化。
- 将“手机摄像头切换”和“哈苏镜头目标视场选择”建模为两种不同状态。

验收：在存在多颗摄像头的测试设备上切换成功；切换后手机预览 Profile 和可模拟镜头列表自动刷新。

### Phase 3：几何计算内核

交付物：可独立测试的 FOV、裁切和屏幕映射模块。

- 实现定焦与变焦端点的焦段解析。
- 实现 X2D2 目标 HFOV/VFOV 与手机预览 HFOV/VFOV 的对比计算。
- 实现指定比例裁切和手机预览可覆盖范围判断。
- 实现 `cover`/`contain` 内容矩形计算。
- 实现屏幕旋转、相机安装角度和归一化坐标转换。
- 实现目标镜头视场框和比例框布局。
- 对边界条件做单元测试：超广角、长焦、正方形、窄屏、横竖屏和无效输入。

验收：同一组输入在不同 UI 尺寸下保持相同的归一化结果；没有除零、NaN、越界或比例反转。

### Phase 4：Canvas 取景框与镜头选择交互

交付物：预览流上的可操作取景辅助层。

- 增加透明 Canvas 并实现按状态重绘。
- 绘制当前镜头框、可选镜头框、比例框、三等分线和中心标记。
- 显示实际焦距、等效焦距、当前比例和“估算/已标定”状态。
- 显示当前手机摄像头、手机预览 FOV、目标哈苏镜头和“可完整模拟/部分模拟/不可完整模拟”状态。
- 提供镜头选择、比例选择、网格开关和辅助线样式。
- 目标视场超过手机预览范围时显示明确提示，不绘制误导性的完整边框。
- 控制重绘频率，避免每帧创建大量临时对象。
- 适配安全区、系统标题栏、横竖屏和折叠态窗口变化。
- 按方向策略实现竖屏默认布局；暂不在本阶段强制窗口横屏。

验收：Canvas 线框不会跟随预览裁切产生漂移；切换镜头/比例后及时更新；连续预览下无明显掉帧或触控延迟。

### Phase 5：多设备标定与可选外部适配

交付物：手机设备 Profile 标定报告和可选的外部输入技术评估。

- 在不同手机/平板和不同后置摄像头上建立 Profile，记录变焦档位和系统裁切差异。
- 用实际参考物验证广角、标准和中长焦目标镜头的框线误差。
- 提供用户水平/垂直 FOV 校准入口，保存每台设备的校准结果。
- 增加横屏沉浸取景模式，切换窗口方向时保存并恢复进入前的方向。
- 验证竖屏 3:2 主框、横屏 3:2 主框以及 4:3/16:9 辅助框在同一组几何输入下的归一化一致性。
- 如果未来需要 X2D2 外部取流，再实现独立 `ExternalPreviewSource`，不改变 MVP 的本机 CameraKit 主链路。

验收：同一目标镜头在已标定设备上的框线误差可复现；不同手机设备不会共用未经验证的 FOV 参数。

### Phase 6：真机校准、稳定性与发布准备

交付物：测试报告、签名配置、发布检查清单。

- 使用至少一台手机和一台平板验证布局与相机能力差异。
- 使用广角、标准和中长焦样本验证视场框。
- 对比实拍照片/视频中的水平和垂直边界，记录误差。
- 验证前后台、来电/系统相机抢占、锁屏、旋转、折叠状态和低电量场景。
- 增加相机错误码和关键状态日志，避免打印敏感画面数据。
- 配置调试/发布签名，并验证安装与升级路径。

验收：形成可复现的设备矩阵和已知限制清单，调试构建与发布构建分别通过。

## 8. 建议的代码目录

```text
entry/src/main/ets/
  camera/
    CameraSource.ets
    CameraKitPreviewSource.ets
    CameraSessionController.ets
  geometry/
    FovCalculator.ets
    PreviewTransform.ets
    OverlayLayoutCalculator.ets
    LensRecommendation.ets
  model/
    LensSpec.ets
    SensorSpec.ets
    PhoneCameraProfile.ets
    ViewFinderState.ets
  data/
    hasselblad-lenses.json5
    sensor-profiles.json5
    phone-camera-profiles.json5
  components/
    PreviewSurface.ets
    OverlayCanvas.ets
    CameraControls.ets
  pages/
    Index.ets
```

目录是实施建议，不要求一次性创建全部文件。应先完成 Phase 0 和 Phase 1，再根据手机 Profile 标定结果扩展多设备支持；`ExternalPreviewSource` 只在未来接入 X2D2 外部画面时创建。

## 9. 测试与验收矩阵

| 类别 | 场景 | 通过标准 |
|------|------|----------|
| 权限 | 首次授权、拒绝、设置页授权 | 状态清晰，拒绝后不访问相机、不崩溃 |
| 预览 | 首帧、持续预览、停止 | 有画面、无拉伸、资源可释放 |
| 设备 | 手机多摄枚举、切换、设备不可用 | 切换无黑屏或有明确错误，预览 Profile 同步更新 |
| 生命周期 | 前后台、锁屏、页面销毁 | 能恢复或安全退出，无残留 Session |
| 方向 | 竖屏、横屏、旋转 | 画面和叠加层方向一致 |
| 取景布局 | 竖屏默认、横屏沉浸模式、方向恢复 | 预览比例不变，控件不遮挡核心取景区 |
| 比例 | 3:2、4:3、16:9、自定义比例 | 框位置和裁切符合配置 |
| 镜头 | 25mm、55mm、90mm、变焦端点 | FOV 框随焦段变化，显示值正确 |
| 覆盖范围 | 目标 FOV 小于、等于、大于手机预览 FOV | 正确显示可完整模拟、部分模拟或不可完整模拟 |
| Canvas | 重绘、快速切换、窗口尺寸变化 | 无漂移、无残影、无明显掉帧 |
| 数据 | 缺失焦段、异常值、无效传感器尺寸 | 有校验和降级，不产生 NaN |
| 标定 | 不同手机摄像头、变焦档位、横竖屏 | Profile 可复现，框线误差在设定阈值内 |
| 发布 | 未签名调试、签名发布 | 明确区分产物，发布包可安装 |

## 10. 主要风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 手机预览 FOV 未知或随变焦变化 | 视场框有系统误差 | 为摄像头/变焦档位保存 PhoneCameraProfile；提供参考物标定 |
| 目标哈苏镜头 FOV 大于手机当前 FOV | 无法显示完整目标框，容易误导选镜 | 提示不可完整模拟，建议切换手机超广角摄像头 |
| 预览流存在裁切、电子防抖或畸变校正 | 视场框有系统误差 | 保存 Preview Transform；提供参考物标定 |
| 镜头表数据含录入异常 | 显示或计算错误 | 规范化数据、来源备注、运行时校验 |
| 手机 CameraKit 画面不是 X2D2 原生画面 | 不能代表 X2D2 的真实成像特征 | UI 标注“模拟视场”，明确用于构图选镜，不宣称原生画质/畸变/景深 |
| 不同设备支持的 Profile 不同 | 某些设备黑屏或启动失败 | 按比例筛选 Profile，准备候选分辨率和降级策略 |
| 后台会回收相机资源 | 回前台黑屏 | 按官方后台恢复流程重新初始化 |
| 未配置签名 | 真机安装/发布受限 | 在 Phase 6 配置签名，不把未签名 HAP 当发布产物 |
| Canvas 与 Surface 计算基准不同 | 取景框漂移 | 统一内容矩形、旋转矩阵和安全区计算 |

## 11. 开发验证命令

```sh
devecocli device list
devecocli emulator list
devecocli build --modules entry --build-mode debug
devecocli run --module entry --device <device-or-emulator>
devecocli log --device <device-or-emulator> --bundle-name com.loho.myapplication --tail 200
```

涉及 CameraKit、权限、XComponent、Canvas 或 API 版本差异时，先使用本地官方文档查询，再实现和构建验证。每次修改 ArkTS 后至少执行：

1. DevEco MCP `check`（当前会话工具可用时）。
2. `devecocli build --modules entry --build-mode debug`。
3. 有设备时执行 `devecocli run` 并检查应用日志。

## 12. 当前进度与下一轮实施顺序

第一轮 MVP 已完成以下基础能力：

1. `module.json5` 已加入 Camera 权限配置和权限文案。
2. `Index.ets` 已接入手机 CameraKit 预览页面骨架。
3. 已实现 CameraKit 相机枚举、启动、停止、释放和手机相机切换。
4. 已建立 18 条哈苏镜头规范化数据和 HFOV/VFOV 字段。
5. 已实现纯函数 FOV 比例换算与模拟视场框。
6. 已接入透明 Canvas、3:2/4:3/16:9 比例框和三等分辅助线。
7. 已将页面调整为“竖屏默认：预览容器 + 独立控制区”，Canvas 不再覆盖控制区。

下一轮按以下顺序推进：

1. 连接真实 HarmonyOS 手机，确认 CameraKit 可用摄像头、Preview Profile、旋转和首帧预览。
2. 测量当前手机各后置摄像头的真实 HFOV/VFOV，替换临时的 `110° × 82°` 参考值。
3. 将 `contentRect` 从当前 Canvas 全部区域升级为 CameraKit 实际可见内容矩形，处理 `cover` 裁切和安全区。
4. 增加横屏沉浸取景模式，并在进入/退出时保存和恢复窗口方向。
5. 增加镜头可模拟状态、手机镜头 FOV 信息和标定入口。
6. 在真机完成广角、标准和中长焦镜头的框线误差验证。

X2D2 外部取流不属于 MVP；只有未来明确需要接收 X2D2 实时画面时，再单独增加外部输入适配层。
